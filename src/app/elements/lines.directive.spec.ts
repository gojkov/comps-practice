import { LinesDirective } from './lines.directive';

describe('LinesDirective', () => {
  it('should create an instance', () => {
    const directive = new LinesDirective();
    expect(directive).toBeTruthy();
  });
});
